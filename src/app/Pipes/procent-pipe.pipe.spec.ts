import { ProcentPipePipe } from './procent-pipe.pipe';

describe('ProcentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
